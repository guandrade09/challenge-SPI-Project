ultralytics>=8.0.0
opencv-python>=4.8.0
requests>=2.31.0
websockets>=12.0
flask>=3.0.0
flask-cors>=4.0.0
shapely>=2.0.0
# av (PyAV) decodifica câmeras RTSP preferindo hardware (NVDEC via hevc_cuvid/h264_cuvid)
<<<<<<< Updated upstream
# (ver ml_service/inference/pyav_camera.py — hoje desativado, a captura usa OpenCV/FFmpeg).
av>=11.0.0
=======
# (ver ml_service/inference/pyav_camera.py — hoje desativado; o RTSP usa VLC sob demanda
# e HTTP/MJPEG/webcam usam OpenCV/FFmpeg).
av>=11.0.0
# python-vlc é só o binding — precisa ter o VLC instalado na máquina (libvlc.dll).
# Usado só para câmeras RTSP: é carregado sob demanda (ver camera.py), então
# HTTP/MJPEG e webcam funcionam sem o VLC instalado.
# (ver ml_service/inference/vlc_camera.py).
python-vlc>=3.0.0
>>>>>>> Stashed changes
